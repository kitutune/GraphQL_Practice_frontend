import { Box, CircularProgress } from "@mui/material";

const Loading = () => {
  return (
    <Box>
      <CircularProgress /> //
      CircularProgressは、マテリアルUIのローディングインジケータコンポーネントです。
    </Box>
  );
};

export default Loading;
